# ⚠️ 已弃用（DEPRECATED）

本目录为 **AI Fuel Planner 早期 Python 测试版**，仅作历史存档。

- **状态**：不再使用、不再维护。
- **正式版本**：`../web/`（纯 JavaScript 静态 Web 应用，功能已完整迁移）。
- **迁移说明**：原 `app.py`、`Dome.py`、`fuel_planner.py`、`test_fuel_planner.py` 中的功能已由 `../web/app.js` 实现（FIT 解析、用户档案、补给策略、单位换算等）。

> 该 Python 测试版的单元测试曾用于早期验证，现不再运行。
